# Copy to .env in this folder: cp .env.example .env
# Never commit .env with real passwords.

POSTGRES_USER=secureacad
POSTGRES_PASSWORD=change_me_in_dev
POSTGRES_DB=secureacad
POSTGRES_PORT=5432

# For Python/SQLAlchemy or other clients on the host machine (adjust password if you change it above).
#DATABASE_URL=postgresql://secureacad:change_me_in_dev@localhost:5432/secureacad

DATABASE_URL=postgresql://postgres:DSS_secureacad@db.mfhuaaxfvdjonyhenmbo.supabase.co:5432/postgres